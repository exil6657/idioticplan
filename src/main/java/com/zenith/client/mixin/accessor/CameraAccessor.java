package com.zenith.client.mixin.accessor;

/**
 * Phase 1 stub — accessor interface for MixinCamera exposing the camera's position,
 * rotation, and move/clip state. Used by Freecam and DroneCam (Phase 18).
 */
public interface CameraAccessor {
    // Phase 18: accessors for setPosition/setRotation/setDetached etc.
}
