package com.zenith.client.mixin.accessor;

/**
 * Phase 1 stub — accessor interface for MixinMouse exposing internal mouse state
 * (accumulated delta values, sensitivity, etc.). The concrete @Accessor methods are
 * added in Phase 3 when MouseSensitivitySimulator is implemented.
 */
public interface MouseAccessor {
    // Phase 3: @Accessor("accumulatedDX") double zenith$getAccumulatedDX();
    // Phase 3: @Accessor("accumulatedDY") double zenith$getAccumulatedDY();
}
